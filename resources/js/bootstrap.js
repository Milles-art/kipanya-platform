/*
 * Kipanya frontend bootstrap.
 *
 * Keep shared browser setup here so app.js remains focused on application
 * behaviour. The current UI does not require a JavaScript dependency, but
 * this entry point is intentionally kept as a stable extension point for
 * future API helpers and CSRF setup.
 */
