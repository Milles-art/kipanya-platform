@extends('layouts.app')
@section('content')
<section class="k-shell flex min-h-[70vh] items-center justify-center py-16"><div class="w-full max-w-md"><div class="k-label text-[var(--accent)]">Kipanya account</div><h1 class="k-display mt-3 text-4xl font-semibold sm:text-5xl">Pick up where you left off.</h1><p class="mt-4 leading-7 k-muted">Sign in with your phone number to keep favorites and watch history synced.</p>@if(session('status'))<div class="mt-6 rounded-xl border k-divider bg-[var(--accent-soft)] p-4 text-sm">{{ session('status') }}</div>@endif<div class="k-card mt-7 p-6 sm:p-7"><form action="{{ route('account.login.request-otp') }}" method="POST" class="space-y-4">@csrf<label class="block text-sm font-semibold">Phone number<input name="phone" value="{{ old('phone') }}" required placeholder="0712 345 678" class="k-control mt-2 w-full rounded-xl px-4 py-3 outline-none"></label>@error('phone')<p class="text-sm text-[var(--danger)]">{{ $message }}</p>@enderror<button class="k-btn k-btn-primary w-full">Send verification code</button></form>@if(session('user_login_phone'))<div class="my-6 border-t k-divider"></div><form action="{{ route('account.login.verify') }}" method="POST" class="space-y-4">@csrf<label class="block text-sm font-semibold">Verification code<input name="code" inputmode="numeric" autocomplete="one-time-code" maxlength="6" required placeholder="6-digit code" class="k-control mt-2 w-full rounded-xl px-4 py-3 tracking-[.3em] outline-none"></label>@error('code')<p class="text-sm text-[var(--danger)]">{{ $message }}</p>@enderror<button class="k-btn k-btn-light w-full">Continue to account</button></form>@endif</div></div></section>
@endsection
