@extends('admin.layout')
@section('content')<div class="flex items-end justify-between"><div><div class="k-label text-black/35">Taxonomy</div><h1 class="mt-2 text-4xl font-semibold tracking-tight">Categories</h1></div><button class="k-btn k-btn-dark">New category</button></div><div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">@foreach($categories as $category)<div class="rounded-2xl border border-black/7 bg-white p-5"><div class="flex items-start justify-between gap-4"><div><h2 class="font-semibold">{{ $category->name }}</h2><p class="mt-1 text-sm text-black/45">{{ $category->slug }}</p></div><span class="text-xs text-black/40">{{ $category->cartoons_count }} titles</span></div><div class="mt-6 text-xs text-black/40">{{ $category->is_active ? 'Active' : 'Inactive' }}</div></div>@endforeach</div><div class="mt-8">{{ $categories->links() }}</div>@endsection