@extends('admin.layout')
@section('content')
<div class="studio-page-head"><div><div class="k-label k-muted">Cartoon Archive · Series</div><h1 class="studio-title">Episodes.</h1><p class="studio-subtitle">Manage every episode attached to the Cartoon Archive.</p></div><a href="{{ route('admin.content') }}" class="k-btn k-btn-light">Back to cartoons</a></div>
<div class="mt-7 grid gap-3">@forelse($episodes as $episode)<a href="{{ route('admin.episodes.edit',[$episode->cartoon,$episode]) }}" class="k-card k-glass flex flex-wrap items-center gap-4 p-4 hover:border-[var(--accent)]"><div class="grid h-11 w-11 shrink-0 place-items-center rounded-xl bg-[var(--accent-soft)] text-sm font-bold text-[var(--accent)]">{{ $episode->episode_number }}</div><div class="min-w-0 flex-1"><div class="truncate text-sm font-semibold">{{ $episode->title }}</div><div class="mt-1 text-xs k-muted">{{ $episode->cartoon->title }} · {{ $episode->youtube_video_id ? 'YouTube linked' : 'No video linked' }}</div></div><span class="k-status {{ $episode->status?->value==='published'?'k-status-published':($episode->status?->value==='archived'?'k-status-archived':'k-status-draft') }}">{{ ucfirst($episode->status?->value ?? 'draft') }}</span><x-icon name="chevron" size="14" class="k-muted"/></a>@empty<div class="k-empty-state"><div class="k-label k-muted">No episodes</div><h2 class="mt-2 text-2xl font-semibold">Your series library is empty.</h2><p class="mx-auto mt-2 max-w-md text-sm k-muted">Open a cartoon and add its first episode.</p></div>@endforelse</div><div class="mt-8">{{ $episodes->links() }}</div>
@endsection
