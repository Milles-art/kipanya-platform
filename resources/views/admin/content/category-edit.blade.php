@extends('admin.layout')
@section('content')
<div class="studio-page-head k-reveal"><div><div class="k-label k-muted">Manage · Taxonomy / {{ $category->name }}</div><h1 class="studio-title">Edit category.</h1><p class="studio-subtitle">Keep the taxonomy clean and useful for discovery.</p></div><a href="{{ route('admin.categories') }}" class="k-btn k-btn-light">Back to categories</a></div>
@if(session('status'))<div class="studio-flash"><span class="studio-flash-dot"></span>{{ session('status') }}</div>@endif
@if($errors->any())<div class="studio-form-errors" role="alert"><strong>Check the fields.</strong><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
<form method="POST" action="{{ route('admin.categories.update',$category) }}" class="mt-7 max-w-2xl k-card k-glass p-6 sm:p-7">@csrf @method('PUT')<div class="grid gap-5"><label class="studio-field"><span>Name</span><input name="name" required maxlength="100" value="{{ old('name',$category->name) }}"></label><label class="studio-field"><span>Description</span><textarea name="description" rows="5" maxlength="500">{{ old('description',$category->description) }}</textarea></label><label class="studio-field"><span>Sort order</span><input type="number" min="0" name="sort_order" value="{{ old('sort_order',$category->sort_order) }}"></label><label class="studio-check"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$category->is_active))><span><strong>Active</strong><small>Allow this category to appear in new content and discovery.</small></span></label><div class="flex flex-wrap justify-between gap-2 pt-2"><div>@if(!$category->cartoons()->exists())<button form="delete-category" class="k-btn k-btn-light text-red-600" type="submit">Delete category</button>@endif</div><div class="flex gap-2"><a href="{{ route('admin.categories') }}" class="k-btn k-btn-light">Cancel</a><button class="k-btn k-btn-primary" type="submit">Save category</button></div></div></div></form>
@if(!$category->cartoons()->exists())<form id="delete-category" onsubmit="return confirm('Delete this category?')" method="POST" action="{{ route('admin.categories.destroy',$category) }}" class="hidden">@csrf @method('DELETE')</form>@endif
@endsection