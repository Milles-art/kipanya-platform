@extends('layouts.app')
@section('content')
<section class="k-shell py-10 sm:py-14"><a href="{{ route('account') }}" class="text-sm k-muted">Back to account</a><div class="mt-5"><div class="k-label text-[var(--accent)]">Your library</div><h1 class="k-display mt-2 text-4xl font-semibold sm:text-6xl">Favorites.</h1><p class="mt-3 k-muted">Stories you've chosen to keep close.</p></div><div class="mt-9 grid gap-x-5 gap-y-9 sm:grid-cols-2 lg:grid-cols-3">@forelse($cartoons as $cartoon)<a href="{{ route('watch',$cartoon) }}" class="group"><div class="aspect-[4/3] overflow-hidden rounded-2xl k-thumb">@if($cartoon->resolved_thumbnail_url)<img src="{{ $cartoon->resolved_thumbnail_url }}" class="h-full w-full object-cover transition duration-500 group-hover:scale-105" alt="{{ $cartoon->title }}">@endif</div><div class="mt-3 font-semibold">{{ $cartoon->title }}</div><div class="mt-1 text-xs k-muted">{{ $cartoon->category->name }}</div></a>@empty<div class="k-card p-8 text-sm k-muted sm:col-span-2 lg:col-span-3">Nothing saved yet.</div>@endforelse</div><div class="mt-10">{{ $cartoons->links() }}</div></section>
@endsection
